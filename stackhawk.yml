app:
  applicationId: ${STACKHAWK_APP_ID} # Use an environment variable
  env: ${STACKHAWK_ENV} # Use an environment variable
  host: ${STACKHAWK_HOST} # Use an environment variable
hawk:
  apiKey: ${STACKHAWK_API_KEY} # Use an environment variable
